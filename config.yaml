sources:
  momentum:
    enabled: true
    weight: 0.4
  reddit:
    enabled: true
    weight: 0.3
    subreddits:
      - wallstreetbets
      - stocks
      - investing
      - options
  news:
    enabled: true
    weight: 0.3
    # Uses Yahoo Finance + MarketWatch (no API key needed)

output:
  top_n: 10
  format: terminal  # terminal, csv, json

cache:
  enabled: true
  ttl_minutes: 60

# Reddit API credentials loaded from .env file
# REDDIT_CLIENT_ID
# REDDIT_CLIENT_SECRET
# REDDIT_USER_AGENT
